{% extends 'base.html.twig' %}

{% block body %}
<h1>{{ name }}</h1>

<h2>Posts ({{ posts | length }})</h2>
<ul>
    {% for post in posts  %}
        <li>{{ post }}</li>
    {% endfor %}
</ul>
{% endblock %}